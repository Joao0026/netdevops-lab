# Test Results

This directory stores generated lab validation reports.

The OSPF failover script writes its latest report to:

```text
results/failover-latest.txt
```

Run the failover test while the lab is running:

```bash
python3 scripts/test_failover.py
```
